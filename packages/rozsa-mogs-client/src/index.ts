import {NetworkClient} from "./network-client";
import {GameClient} from "./game-client";
import {SocketMessage} from "./socket-message";

export { NetworkClient, GameClient, SocketMessage }

export default {
    NetworkClient,
    SocketMessage
}